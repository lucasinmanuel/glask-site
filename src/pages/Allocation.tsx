import { Outlet, Link } from "react-router-dom"

function Allocation() {
  return (
    <main className="App">
        <div className="container">
            <div>Allocation</div>
        </div>
    </main>
  );
}

export default Allocation;